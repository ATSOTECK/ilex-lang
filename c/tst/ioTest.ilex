// Both of these work.
//use * from <io>
use { input } from <io>

use <math as M>

fn test() {
    str := input('Enter something: ')
    println('You entered', str)
    
    num := str.toNumber() ?? 0
    
    println(num + 1)
    
    println(M::pi)
}

/-
use <io>

fn test() {
    str := io::input('Enter something: ')
    println('You entered', str)
    
    num := str.toNumber() ?? 0
    
    println(num + 1)
}
-/

test()
