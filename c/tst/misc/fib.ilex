use { milliseconds, seconds } from <ilex>

fn fib(n) {
    if (n < 2) {
        return n
    }

    return fib(n - 2) + fib(n - 1)
}

fibDepth ::= 40
println('fib({fibDepth}) = {fib(fibDepth)}')
println('{seconds()} seconds')
println('{milliseconds()} ms')
