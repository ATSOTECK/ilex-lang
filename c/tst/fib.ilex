fn fib(n) {
    if (n < 2) {
        return n;
    }

    return fib(n - 2) + fib(n - 1);
}

fn main() {
    start := seconds();
    println(fib(40));
    println(seconds() - start, "seconds");
}

main();
