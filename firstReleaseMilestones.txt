map datatype
set datatype
shallow/deep copy for arrays 
(done) file IO
HTTP lib
SQL lib
use user made scripts
16 bit?
(done) switch fallthrough
(done) annonomys functions
fix GC bugs
readPassword function for io

arr.map(fn |x| x * 2)
arr.map(fn |x| -> {
    x ** 2
})
someFunc(fn -> println('yeet'))
